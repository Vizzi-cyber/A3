# LearnLab 算法升级方案（AIC 算法创新赛版）

> **依据**：AIC 2026 算法创新赛 · "AI+学科交叉"赛道评分标准（创新性 20 / 需求分析 15 / 方案可行性 20 / 项目实施 20 / 应用效果 20 / 总结与展望 5）
> **对象**：LearnLab 现有 5 大算法资产（backend/app/algorithms/ 4 模块 + 前端 MNA 仿真器）
> **原则**：每个升级都必须"看得见、说得清、可验证"——有论文出处、有公式、有对照实验设计
> **日期**：2026 年 8 月

---

## 0. 现有算法资产盘点（先看清家底）

| 算法模块 | 文件 | 现状实现 | 评审视角的问题 |
|---|---|---|---|
| ADPP 路径规划 | `path_planning_dag.py` (508行) | CPM 关键度 + **简化 BKT** + 六因子成本 + 加权拓扑排序 + 规则式阶段划分/动态调整 | ①"简化 BKT"实际只是概率合并公式 `1-Π(1-pᵢ)`，**无学习率/猜测率/失误率/先验参数，无 EM 估计**——评审一眼能看出不是真 BKT；②权重全是人工经验值 |
| 效果评估 | `effect_evaluation.py` (226行) | 正确率 + 最近5次加权平均掌握度 + 线性回归斜率 + HHI + **线性外推预测** | ①掌握度=加权平均分，无测量学模型；②"增加间隔重复练习频次"**只是字符串，无记忆算法**；③预测是简单外推 |
| 趋势分析 | `trend_analysis.py` (314行) | 6 因子**人工权重**线性加权 + 线性外推 3 天预测 + 规则阈值预警 | 权重 0.35/0.15/0.15/0.15/0.05/0.15 全靠经验，无学习；预测无置信度 |
| 加权匹配 | `weighted_matching.py` (286行) | 5 维人工权重线性加权 + 规则子评分 | 纯规则打分，**无探索-利用**，无在线学习 |
| MNA 电路仿真 | `mna-solver.ts` (400行) | 并查集 + MNA 方程组 + 列主元高斯消元（直流稳态） | 数值方法可升级；技术方案自认"暂态分析待增强" |

> **核心判断**：现有算法"工程上能用、演示上够用"，但作为**算法创新赛**参赛作品，算法层学术含量不足——评分表里"创新性 20 分"和"应用效果 20 分"都要求有"方法"和"验证"。升级不是推翻重来，而是**在每个模块上叠加一个可引用的经典/前沿算法**。

---

## 1. 升级总框架：四层算法架构

```
┌────────────────────────────────────────────────────────────────────┐
│  【测量层】IRT 1PL/2PL 认知诊断（能力 θ + 题目难度 b 统一尺度）       │
│    替换：加权平均掌握度、人工难度 1-5 分级                          │
├────────────────────────────────────────────────────────────────────┤
│  【建模层】完整 BKT（EM 估计）→ GKT 图知识追踪 → 神经符号 DKT        │
│    替换：简化 BKT 概率合并公式                                       │
├────────────────────────────────────────────────────────────────────┤
│  【记忆层】FSRS / SM-17 双组分记忆（D/S/R 三变量）                  │
│    替换："增加间隔重复练习频次"字符串                                │
├────────────────────────────────────────────────────────────────────┤
│  【决策层】Thompson Sampling / EduQate（探索-利用 + 网络化决策）    │
│    替换：规则阈值（50/70/90 分档）、人工权重排序                    │
├────────────────────────────────────────────────────────────────────┤
│  【解释层】算法输出注入 12 个 LLM 智能体（Agentic KT 叙事）          │
│    已有：Tutor/ErrorCatcher/ResourceGenerator 接口                  │
└────────────────────────────────────────────────────────────────────┘
```

**一句话定位升级后的 LearnLab**："IRT 测量、BKT/GKT 建模、FSRS 记忆调度、MAB 决策、LLM 解释"的五层算法闭环——每层都有经典或前沿出处。

---

## 2. 分模块升级方案

### 2.1 ADPP 路径规划 → ADPP-Pro（重点，改动最大、加分最多）

**现状痛点**：简化 BKT 无参数估计；权重人工设定；动态调整靠阈值。

**升级 1：完整贝叶斯知识追踪（BKT，EM 参数估计）**
- 每个知识点独立隐马尔可夫模型，4 参数：先验掌握概率 P(L₀)、学习率 P(T)、猜测率 P(G)、失误率 P(S)
- 答题序列驱动后验更新：P(Lₙ|response) → 预测下一题答对概率
- 参数用 EM 算法从历史答题数据估计（不再手拍）
- **引用**：Corbett & Anderson (1995)
- **实现**：纯 Python + numpy，无新依赖，约 150-200 行
- **可验证**：BKT 预测答对概率 vs 实际答题，算 AUC——**对比"简化 BKT"的预测准确率，就是现成的"应用效果"数据**

**升级 2：图知识追踪（GKT）**（第 3 批，可选）
- 知识点 DAG 就是现成的图（`reverse_graph` 已构建）
- GCN 聚合邻居掌握度更新学生状态（GCN+GRU）
- **引用**：GKT, IEEE/WIC/ACM WI 2019（注意无 arXiv 预印本）
- **实现**：需引入 torch 或自实现 GCN（矩阵运算即可），数据量小

**升级 3：决策层——路径动态调整从规则阈值 → Thompson Sampling**
- 现有 `adjust_path` 的 50/70/90 分档 → 每个候选调整策略一个 Beta 分布，按历史收益采样
- **引用**：ZPDES（265 名儿童 RCT 优于人工课程，arXiv:2402.01669）
- **实现**：几十行，numpy 即可

**升级 4：六因子成本模型参数化（IRT 对接）**
- 难度系数 {1:0.7, 2:0.9, ...} 人工表 → 使用 IRT 估计的题目难度 b（见 2.2）
- 保持公式形态（可解释），只换输入来源

### 2.2 效果评估 → 认知诊断 + 记忆模型

**升级 1：IRT 1PL/2PL 能力估计**
- 掌握度从"最近5次加权平均分" → IRT 能力值 θ（EM 估计）
- 公式直接写进技术方案：P(答对) = 1/(1+e^-a(θ-b))
- 题目难度从人工 1-5 → 数据标定 b 值
- **引用**：Rasch (1960)；py-irt 有开源实现
- **可验证**：IRT 估计能力 vs 实际测验得分的相关性（效度指标）

**升级 2：FSRS/SM-17 双组分记忆调度（补"艾宾浩斯"的真实算法）**
- 每个知识点维护 D（难度）/S（稳定性）/R（可提取性）三变量
- 复习后更新三变量，算出下次最优复习时刻
- 插入 ADPP 路径的 `review` 阶段；前端"复习提醒/每日复习队列"取数
- **引用**：Ye et al. KDD 2022 / Liu et al. TKDE 2023（FSRS）；SuperMemo 17（SM-17）
- **可验证**：FSRS 复习 vs 固定间隔复习的记忆保持率对照

**升级 3（可选）：预测模型**
- 线性外推 → 先加置信区间（正态假设），数据充足后上 DKT/LSTM
- 技术方案里写清"数据规模规划"，别承诺已实现

### 2.3 趋势分析 → 可学习的掉队预警

**升级 1：权重学习化**
- 6 因子人工权重 → 用历史数据训练逻辑回归/GBDT（特征=6 因子，标签=是否掉队/测验下滑）
- 输出的"预警概率"比规则阈值更有说服力
- **引用**：教育数据挖掘经典方向"辍学/掉队预测"（EdNet 有基准）

**升级 2：预测加不确定性**
- 未来 3 天掌握度预测 → 输出点估计 + 置信区间（体现统计严谨）

### 2.4 加权匹配 → 上下文多臂老虎机

**升级 1：保留打分先验 + 叠加 Thompson Sampling/LinUCB 探索层**
- 现有 5 维加权打分作为"先验收益"，MAB 负责探索-利用
- 每日练习选题（`daily_quiz.py`）从固定规则 → MAB 选题
- **引用**：ZPDES（RCT 验证）、Thompson (1933)
- **实现**：几十行

### 2.5 MNA 电路仿真 → 学科算法深度升级（AI+学科交叉的差异化亮点）

**升级 1：数值方法**
- 列主元高斯消元 → LU 分解（复用因子）+ 稀疏求解（大电路）
- 直流稳态 → **暂态分析**（RK4 龙格-库塔数值积分，电容/电感充放电波形）
- 技术方案"暂态分析待增强"的客观不足直接补上

**升级 2：AI 叠加**
- 现有 3 个故障模板 + 规则判定 → 保留规则（快速判定）+ AI 深度分析（已有）+ **AI 生成故障/设计助手**（第 3 批）

---

## 3. 跨层叙事：算法层 → LLM 智能体（Agentic KT 混合架构）

**升级后的完整故事**（直接引用支撑）：
1. 算法层输出**掌握度向量**（IRT θ + BKT 后验概率 + FSRS R 值）→ 注入 12 个智能体 prompt
2. Tutor 辅导按薄弱点讲解、ErrorCatcher 诊断结合 BKT 概率、ResourceGenerator 按记忆状态生成
3. 答题结果回流 → 更新 BKT/IRT/FSRS 参数 → 闭环

**论文背书**（放进参考文献）：
- **纯 LLM 做不了知识追踪**（DKT AUC 0.83 胜过零样本/微调 LLM，arXiv:2512.23036）→ 论证"必须算法+LLM 混合"的架构合理性
- **Agentic Knowledge Tracing**（多智能体 BKT 流水线预测效度 3 倍于单 LLM，arXiv:2606.25358）→ 与 12Agent 体系同构，直接对标

---

## 4. 升级优先级与实施批次

| 批次 | 内容 | 工作量 | AIC 得分点 | 数据需求 |
|---|---|---|---|---|
| **第 1 批（必做）** | 完整 BKT（EM 估计）+ IRT 1PL/2PL + FSRS 记忆调度 | 3-5 天 | 创新性 20 + 应用效果 20 | 小（现有 quiz_results 即可） |
| **第 2 批（必做）** | Thompson Sampling 选题 + 路径调整 MAB 化 | 1-2 天 | 方案可行性 20 | 小 |
| **第 3 批（加分）** | GKT 图知识追踪 + 神经符号 DKT（Responsible-DKT 思路） | 5-7 天 | 创新性 20（前沿叙事） | 中 |
| **第 4 批（学科亮点）** | MNA 暂态分析（RK4）+ LU 分解 | 3-4 天 | 学科算法深度、应用效果 | 无（纯前端） |
| **第 5 批（收尾）** | Agentic KT 叙事 + 算法层→智能体接口 + 参考文献入册 | 1-2 天 | 需求分析/创新性/总结 | 无 |

## 5. 效果验证设计（AIC "应用效果 20 分"直接素材）

| 对照实验 | 实验组 | 对照组 | 指标 |
|---|---|---|---|
| 知识追踪精度 | 完整 BKT（EM） | 简化 BKT（现状） | 预测 AUC / 准确率 |
| 复习效果 | FSRS 调度 | 固定间隔复习 | 7 天记忆保持率 |
| 测量效度 | IRT 能力 θ | 加权平均分 | 与后测成绩的相关性 |
| 选题效果 | Thompson Sampling | 固定规则选题 | 完成率 / 掌握度提升 |
| 暂态仿真 | RK4 暂态分析 | 仅直流稳态 | 波形正确性（对比理论解） |

> 每项实验都能产出"传统方法 vs 算法方法"的量化对比表——这正是 AIC 评分表里"对比传统学科建设方式的优势"和"效果验证依据"的直接答案。

---

## 6. 参考文献补充清单（可直接入册，替换"待补充 15-20 篇"）

1. Corbett & Anderson (1995). Knowledge Tracing. *User Modeling and User-Adapted Interaction*.
2. Piech et al. (2015). Deep Knowledge Tracing. *NeurIPS*. arXiv:1506.05908
3. Rasch (1960). Probabilistic Models for Some Intelligence and Attainment Tests.
4. Ye et al. (2022). A Stochastic Shortest Path Algorithm for Optimizing Spaced Repetition Scheduling. *KDD*.
5. Liu et al. (2023). Optimizing Spaced Repetition Schedule by Capturing the Dynamics of Memory. *TKDE*.
6. ZPDES (2024). 265 名儿童 RCT 验证 MAB 选题优于人工课程. arXiv:2402.01669
7. EduQate (2024). Education Network Restless Multi-armed Bandits. *NeurIPS*. arXiv:2406.14122
8. GKT (2019). Graph-based Knowledge Tracing. IEEE/WIC/ACM WI 2019.
9. Responsible-DKT (2026). Neuro-symbolic Knowledge Tracing. arXiv:2604.08263
10. Agentic Knowledge Tracing (2026). IEEE CoG. arXiv:2606.25358
11. Hooshyar et al. (2025). 纯 LLM 无法可靠做知识追踪. arXiv:2512.23036
12. AKT (2020). Context-Aware Attentive Knowledge Tracing. *KDD*. arXiv:2007.12324
13. SAINT+ (2021). Transformer-based Knowledge Tracing. *LAK*. arXiv:2010.12042
14. KCD (2025). Large Language Model-based Cognitive Diagnosis. arXiv:2502.05556
15. 《电路分析基础》教材（MNA 节点电压法、暂态分析）
16. 教育部《新工科建设宣言》(2017) /《教育数字化战略行动》
