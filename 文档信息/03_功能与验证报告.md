# 功能与验证报告

## 1. 当前实际验证结果

| 验证项 | 结果 | 命令 |
|---|---:|---|
| AI 算法专项（BKT/IRT/FSRS/MAB/GKT/NCD/五层接线/趋势学习器/匹配探索） | 77/77 | `python -X utf8 scripts/verify_ai_algorithms.py` |
| AIC 功能回归 | 29/29 | `python -X utf8 scripts/verify_aic_features.py` |
| 算法接线 API 冒烟（含演示库真实训练 IRT/GKT/趋势学习器） | 16/16 | `python -X utf8 scripts/verify_p0_wiring_api.py` |
| 全链路数据流 | 23/23 | `python -X utf8 scripts/verify_dataflow.py` |
| Agent/LLM 专项 | 23/23 | `python -X utf8 scripts/verify_agent_llm.py` |
| 全路由冒烟 | 209 路由 0 崩溃 | `python -X utf8 scripts/verify_all_routes.py`（需先启动 uvicorn） |
| MNA 数值对照（RC/RL/LC 解析解） | 9/9 | `cd frontend && npm run test:mna` |
| 前端构建 | 通过 | `cd frontend && npm run build` |

## 2. 已验证功能

- 五层算法闭环接线：IRT θ 掌握度（dashboard/trend）、IRT b 学习成本、BKT 掌握度（AUC 0.837）、GKT 图卷积传播、FSRS 记忆小节、MAB 路径调整策略/每日选题/匹配探索层（含收益闭环回传）。
- 趋势权重学习器训练（`POST /algorithms/trend/train`）与掉队预警概率输出；GKT 自监督训练（`POST /algorithms/gkt/train`）。
- RK4 暂态分析：电容充放电/电感电流波形，数值对照解析解通过。
- Dashboard、课程知识列表、每日练习核心接口。
- 多模型 Provider 注册和无 Key 跳过逻辑。
- 功能使用频率统计及学生/教师权限隔离。
- 课程元数据、跨课程依赖、跨学科路径和 DAG 无环。
- 实验行为上报和统计权限。
- 电路故障诊断及普通电路分析。
- 学习行为、测验、画像、路径、趋势、教师报告数据流。
- Agent 反思、缓存、JSON 容错、降级和防幻觉守卫。

## 3. 已实现但依赖运行环境

- 真实 LLM 调用：需要至少一个有效 Provider API Key。
- DeepSeek、智谱、OpenAI、MiMo：需要对应 Key，未配置时不会实际调用。
- WebSocket 辅导：需要后端服务和有效 Token。
- OCR、PPT、生图：需要对应服务配置或素材服务可用。
- 前端页面交互：需要浏览器、前后端服务和数据库状态一致。

## 4. 仍需人工验收或补充

- 全量 Playwright E2E 在当前工作区的完整重新执行。
- 所有教师页面的视觉和交互走查。
- 实验报告下载、PPT 生成、OCR 上传的真实外部服务验收。
- 真实学生试点和实验组/对照组前后测。

## 5. 仍未完成的交付材料

- 团队成员专业、年级、分工最终确认（建议框架已在技术方案（五）3 给出）。
- 真实试点数据报告（对照实验设计已就绪，见技术方案（六）3）。
- 最终 PPT（需按 AIC 六维评分从软件杯版重排）和演示视频（5-8 分钟）。

已完成：AIC 技术方案八大部分（含提交材料对照表、22 篇参考文献、经济可行性、推广路径、伦理与知识产权声明，见 `AIC技术方案_LearnLab.md`）。

## 6. 验证注意事项

Windows 终端运行 Python 验证脚本时使用 `-X utf8`，否则 GBK 终端可能无法打印勾选符号；该编码问题不代表功能验证失败。
